syntax = "proto3";
package rpcExample;

message request
{
    int64 a = 1;
    int64 b = 2;    
}

message result
{
    int64 c = 1;
}

service Service{
    rpc Add (request) returns (result);
    rpc sub (request) returns (result);
}